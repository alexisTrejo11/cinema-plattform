syntax = "proto3";

package cinema.ticket.v1;

// Minimal surface so this process can expose a gRPC port (health / discovery).
service TicketService {
  rpc Ping(PingRequest) returns (PingResponse);
}

message PingRequest {}

message PingResponse {
  string service = 1;
  string version = 2;
}
